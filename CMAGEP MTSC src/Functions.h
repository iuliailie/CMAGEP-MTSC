#ifndef FUNCTIONS_H
#define FUNCTIONS_H


class Functions
{
public:
    Functions();
    double Plus(double a, double b);
    double Minus(double a,const double b);
    double Divide(double a,const double b);
    double Times(double a,const double b);

    void test()
    {
        /*

        cout<<"................."<<endl;

        for(int i=0; i<SelectedChromosomes.at(0)->numberOfGenes; i++)
        {
            cout<<SelectedChromosomes.at(0)->Genes->at(i)->toString()<<endl;
        }
        cout<<endl;
        for(int i=0; i<SelectedChromosomes.at(1)->numberOfGenes; i++)
        {
            cout<<SelectedChromosomes.at(1)->Genes->at(i)->toString()<<endl;
        }

         SelectedChromosomes.at(1)->GeneSwap(SelectedChromosomes.at(0));
         cout<<"......................................."<<endl;

          for(int i=0; i<SelectedChromosomes.at(0)->numberOfGenes; i++)
        {
            cout<<SelectedChromosomes.at(0)->Genes->at(i)->toString()<<endl;
        }
        cout<<endl;

        for(int i=0; i<SelectedChromosomes.at(1)->numberOfGenes; i++)
        {
            cout<<SelectedChromosomes.at(1)->Genes->at(i)->toString()<<endl;
        }
        */
    }


};

#endif // FUNCTIONS_H
